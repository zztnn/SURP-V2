# SURP Web — variables de entorno frontend.
# Copiar a .env.local (no commiteable).

# URL pública del backend SURP API (se inyecta al bundle vía NEXT_PUBLIC_*).
NEXT_PUBLIC_API_URL=http://localhost:3201
